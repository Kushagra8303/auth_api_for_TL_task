MONGO_URI=mongodb+srv://mrkushagrasrivastava8303_db_user:authApi%40123@authcluster.0ptrpvr.mongodb.net/authDB

# port for local development (Render will provide its own PORT automatically)
PORT=5000

# base URL of the app. on Render you can set this to the service URL
# locally it defaults to http://localhost:5000
# when deployed you can copy the Render address here or use
# RENDER_EXTERNAL_URL instead
APP_URL=https://auth-api-for-tl-task.onrender.com